"""Rendering — symétrique de parsing : data → document.

Cf. CLAUDE_tome2.md. L'arborescence cible :
    rendering/pdf/     — annotations + rebuild PDF traduit
    rendering/word/    — rebuild .docx traduit
    rendering/pptx/    — rebuild .pptx traduit
    rendering/xlsx/    — rebuild .xlsx traduit
"""
